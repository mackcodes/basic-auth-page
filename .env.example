# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/login-auth

# JWT signing key
JWT_SECRET=replace-with-a-strong-random-secret

# Optional app port (currently app.js defaults to 3000)
PORT=3000
